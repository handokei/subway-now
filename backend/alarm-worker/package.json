{
  "name": "subway-now-alarm-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that polls Seoul Arrival API for active trips and fires APNs silent push when ETA changes or alarm fires.",
  "type": "module",
  "main": "src/index.ts",
  "engines": {
    "node": ">=23.6"
  },
  "scripts": {
    "dev": "wrangler dev --config wrangler.toml",
    "deploy": "bash scripts/deploy.sh",
    "test": "vitest run --coverage",
    "test:watch": "vitest",
    "type-check": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 migrations apply subway-now-db --local --config wrangler.toml",
    "db:migrate:remote": "wrangler d1 migrations apply subway-now-db --remote --config wrangler.toml",
    "check:trip-metrics-regression": "node scripts/checkTripMetricsRegression.mjs"
  },
  "dependencies": {
    "@sentry/cloudflare": "^10.59.0",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.6.0",
    "jose": "^5.9.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "@vitest/coverage-v8": "^2.1.9",
    "drizzle-kit": "^0.31.10",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "wrangler": "^4.105.0"
  }
}
